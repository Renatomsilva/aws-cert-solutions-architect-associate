name: Call a reusable workflow

on: [push]

jobs:
  call-workflow:
    uses: Renatomsilva/aws-cert-solutions-architect-associate/.github/workflows/function.yml@v1
    with:
      config-path: .github/function.yml
    secrets:
      token: ${{ secrets.envPAT }}